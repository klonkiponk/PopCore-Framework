<!--
function popup(link) {
  if (!window.focus) return true;
  var href;

  if (typeof(link) == 'string') {
     href = link;
  } else {
     href = link.href; 
  }
  window.open(href,'Bildergallery', 'width=800,height=600,scrollbars=yes');
  return false;
}
-->
